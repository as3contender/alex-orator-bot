# Alex Orator Bot - Deployment Configuration
# Copy this file to deploy.env and fill with your actual values

# =============================================================================
# SERVER CONNECTION
# =============================================================================

# SSH connection details
REMOTE_HOST=64.227.69.138
REMOTE_USER=root
SSH_KEY_PATH=~/.ssh/id_ed25519_do_cloverdash-bot

# Deployment directory on server
REMOTE_DEPLOY_DIR=/opt/alex-orator-bot

# =============================================================================
# DEPLOYMENT OPTIONS
# =============================================================================

# What to deploy (true/false)
DEPLOY_BACKEND=true
DEPLOY_BOT=true
DEPLOY_DATABASES=true

# =============================================================================
# SECURITY KEYS (GENERATE NEW ONES FOR PRODUCTION!)
# =============================================================================

# Application secrets (generate with: python3 -c "import secrets, base64; print(base64.b64encode(secrets.token_bytes(32)).decode())")
JWT_SECRET_KEY=sot8=OOF7lR8qwcUZNdzqRA|Ym+*:IP.Z:g9*v6GD=k5[ZBxSMkEDA5M6LR|,]H|
SECRET_KEY=sHbSbAGe5a4ovqvcmibKr2BaFYC5n6zkiO6DxKGY09k=

# =============================================================================
# DATABASE PASSWORDS
# =============================================================================

# PostgreSQL passwords (use strong passwords in production)
APP_DB_PASSWORD=(kI|;el.%uMF7zsbjCKq

# =============================================================================
# TELEGRAM BOT
# =============================================================================

# Telegram Bot Token (get from @BotFather)
TELEGRAM_TOKEN=8492044352:AAF7z66M__Htet5gI-MgB_TK_PnsqLrmazY

# =============================================================================
# PRODUCTION SETTINGS
# =============================================================================

# Logging
LOG_LEVEL=INFO
DEBUG=false

# CORS (adjust for your domain)
CORS_ORIGINS=["https://your-domain.com"]

# JWT settings
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# =============================================================================
# EXAMPLE COMMANDS
# =============================================================================

# Deploy everything:
# ./deploy_alex_orator.sh

# Deploy only backend:
# ./deploy_alex_orator.sh --backend-only

# Deploy only bot:
# ./deploy_alex_orator.sh --bot-only

# Deploy without databases (use external DB):
# ./deploy_alex_orator.sh --no-db

# Deploy with custom server:
# ./deploy_alex_orator.sh -h your-server.com -u ubuntu -k ~/.ssh/your-key
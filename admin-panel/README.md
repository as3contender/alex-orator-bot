# 👨🏻‍💼 Admin Panel - Alex Orator Bot

Админ-панель для управления контентом бота Alex Orator, построенная на Streamlit.

## ✨ Возможности

- **🔐 Аутентификация** - безопасный вход по логину/паролю с поддержкой ролей
- **👥 Управление пользователями** - создание, редактирование, удаление пользователей
- **📝 Управление контентом** - полный CRUD для таблицы `bot_content`
- **📱 Многострочный текст** - поддержка длинных сообщений
- **🌐 Мультиязычность** - поддержка ru, en, kz
- **📊 Статистика** - детальная аналитика контента
- **🎭 Система ролей** - гибкое разделение доступа (super_admin, admin, moderator, user)
- **🗄️ Роли из БД** - динамическое определение ролей пользователей

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Настройка окружения
Создайте файл `env_config.py` на основе `env_config.example.py`:
```bash
cp env_config.example.py env_config.py
```

Затем отредактируйте `env_config.py` и укажите реальные значения:
- `ADMIN_DB_PASSWORD` - пароль от базы данных
- `ADMIN_DB_USER` - пользователь базы данных
- `ADMIN_DB_HOST` - хост базы данных
- `ADMIN_DB_PORT` - порт базы данных
- `ADMIN_DB_NAME` - имя базы данных

### 3. Запуск
```bash
streamlit run admin_app.py
```

### 4. Доступ
Откройте браузер: **http://localhost:8501**

**Логин:** `admin`  
**Пароль:** `admin123`

**Тестовые пользователи с разными ролями:**
- `super_admin` / `super123` - Супер-администратор
- `moderator` / `mod123` - Модератор
- `admin2` / `admin456` - Администратор

**Пользователи из таблицы users:**
- `adam` / `adam123` - обычный пользователь
- `den` / `den123` - обычный пользователь

## 📋 Функциональность

## 🎭 Система ролей

### 👑 Супер-администратор (`super_admin`)
- **Доступ:** Полное управление системой
- **Вкладки:** "Управление контентом", "Пользователи"
- **Возможности:** Создание, редактирование, удаление контента и пользователей

### 👨🏻‍💼 Администратор (`admin`)
- **Доступ:** Полное управление контентом и пользователями
- **Вкладки:** "Управление контентом", "Пользователи"
- **Возможности:** Создание, редактирование, удаление контента и пользователей

### 🛡️ Модератор (`moderator`)
- **Доступ:** Ограниченное управление контентом
- **Вкладки:** "Управление контентом", "Пользователи"
- **Возможности:** Просмотр и редактирование контента, просмотр пользователей

### 👤 Обычные пользователи (`user`)
- **Доступ:** Только просмотр контента и пользователей
- **Вкладки:** "Просмотр контента", "Просмотр пользователей"
- **Возможности:** Просмотр данных без возможности редактирования

### 📝 Управление контентом
Админка предоставляет 4 основные вкладки:

#### 📋 Просмотр
- Фильтрация по языку и статусу
- Таблица с основной информацией (ключ, создан, обновлен, длина текста)
- Детальный просмотр каждого элемента
- Кнопки редактирования и активации/деактивации (для администраторов и модераторов)

#### ➕ Создать
- Создание нового контента с валидацией
- Выбор языка (ru, en, kz)
- Настройка статуса (активен/неактивен)
- Предварительный просмотр текста
- Упрощенная форма без лишних полей

#### ✏️ Редактировать
- Изменение существующего контента
- Предварительный просмотр изменений
- Сохранение, отмена или удаление

#### 📊 Статистика
- Общие метрики (всего записей, активных, неактивных)
- Статистика по языкам
- Топ самых длинных сообщений
- Общее количество символов

### 👥 Управление пользователями (только для администраторов)
- **Создание пользователей** - добавление новых пользователей с хешированием паролей
- **Редактирование** - изменение данных существующих пользователей
- **Удаление** - безопасное удаление пользователей
- **Просмотр списка** - таблица всех пользователей с фильтрацией и ролями
- **Статистика** - общее количество, активные/неактивные пользователи, распределение по ролям
- **Управление ролями** - назначение ролей (super_admin, admin, moderator, user) при создании
- **Роли из БД** - роли сохраняются в таблице users и подхватываются автоматически

### 👤 Просмотр пользователей (для всех пользователей)
- **Список пользователей** - таблица с основной информацией и ролями
- **Статистика** - метрики по пользователям, распределение по ролям
- **Последние регистрации** - топ-5 новых пользователей
- **Информация о правах** - что доступно и что ограничено
- **Роли пользователей** - отображение ролей из базы данных с красивыми иконками

## 🗄️ База данных

### Подключение
- **Тип:** PostgreSQL
- **Хост:** localhost
- **Порт:** 5434
- **База:** app_db
- **Пользователь:** alex_orator

### Таблица bot_content
```sql
CREATE TABLE bot_content (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    content_key VARCHAR(255) NOT NULL,
    language VARCHAR(10) NOT NULL,
    content_text TEXT NOT NULL,
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(content_key, language)
);
```

## 🔧 Технические детали

### Архитектура
- **Frontend:** Streamlit
- **Backend:** Python async/await
- **База данных:** PostgreSQL + asyncpg
- **Аутентификация:** JWT токены

### Файловая структура
```
admin_panel/
├── admin_app.py          # Главное приложение
├── content_page.py       # Страница управления контентом
├── users_management.py   # Управление пользователями (для админов)
├── users_view.py         # Просмотр пользователей (для обычных пользователей)
├── auth.py              # Аутентификация и система ролей
├── database.py          # Работа с БД
├── add_test_users.py    # Скрипт добавления тестовых пользователей
├── requirements.txt     # Зависимости
├── README.md           # Документация
└── ADMIN_SETUP.md      # Настройка администраторов
```

### Безопасность
- **Хеширование паролей:** SHA-256 для пользователей из БД
- **Система ролей:** Гибкое разделение доступа (super_admin, admin, moderator, user)
- **JWT токены:** Для сессий администраторов
- **Валидация данных:** Проверка входных данных
- **Защита от SQL-инъекций:** Использование параметризованных запросов
- **Аутентификация:** Поддержка пользователей из БД и системного администратора
- **Роли из БД:** Динамическое определение ролей пользователей
- **Конфигурация:** Пароли и настройки вынесены в отдельный файл `env_config.py` (не добавляется в git)

## 🆘 Решение проблем

### Ошибки подключения к БД
1. Проверьте статус Docker: `docker ps`
2. Убедитесь, что PostgreSQL запущен на порту 5434
3. Проверьте переменные окружения в `env_config.py`

### Проблемы с аутентификацией
1. Проверьте логин/пароль: admin/admin123
2. Очистите кэш браузера
3. Перезапустите приложение

### Ошибки event loop
1. Убедитесь, что используете `asyncio.run()`
2. Не создавайте новые event loop'ы вручную
3. Перезапустите Streamlit приложение

## 📚 Дополнительные материалы

- [SUCCESS.md](SUCCESS.md) - Статус выполнения задач
- [CONTENT_PAGE_GUIDE.md](CONTENT_PAGE_GUIDE.md) - Подробное руководство по управлению контентом
- [env_config.example.py](env_config.example.py) - Пример конфигурации

## 🧪 Тестирование системы ролей

### Добавление тестовых пользователей
Для тестирования системы ролей используйте скрипт:

```bash
python add_test_users.py
```

Этот скрипт создаст пользователей с разными ролями:
- `super_admin` - Супер-администратор
- `moderator` - Модератор  
- `admin2` - Администратор

### Проверка ролей
1. Войдите под разными пользователями
2. Проверьте доступные вкладки и функции
3. Убедитесь, что роли отображаются корректно

## 🎯 Особенности реализации

- **Система ролей** - разделение доступа администраторов и пользователей
- **Управление пользователями** - полный CRUD для таблицы `users`
- **Упрощенный интерфейс** - адаптивные вкладки в зависимости от роли
- **Стабильное подключение** - исправлены ошибки event loop
- **Многострочный текст** - полная поддержка длинных сообщений
- **Фильтрация** - по языку и статусу
- **Статистика** - детальная аналитика контента и пользователей
- **Красивые приветствия** - персонализированные сообщения без дублирования

---

**Статус:** ✅ ГОТОВО К ИСПОЛЬЗОВАНИЮ  
**Версия:** 3.0 (с системой ролей)  
**Дата:** 2025-08-21



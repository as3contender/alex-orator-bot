# Пример файла переменных окружения для админ-панели
# Скопируйте этот файл в .env и заполните реальными значениями

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=app_db
DB_USER=alex_orator
APP_DB_PASSWORD=your-secure-database-password-here

# Security Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-minimum-32-characters
ADMIN_DEFAULT_USERNAME=admin
ADMIN_DEFAULT_PASSWORD=your-secure-admin-password-here

# Security Settings
MAX_LOGIN_ATTEMPTS=5
LOGIN_TIMEOUT_MINUTES=15
SESSION_TIMEOUT_MINUTES=30
BCRYPT_ROUNDS=12

# Streamlit Configuration
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_SERVER_ENABLE_CORS=false
STREAMLIT_SERVER_ENABLE_XSRF_PROTECTION=true

# Logging
LOG_LEVEL=INFO

# ⚠️ ВАЖНО:
# 1. Никогда не коммитьте реальные пароли в Git
# 2. Используйте сильные, уникальные пароли (минимум 16 символов)
# 3. JWT_SECRET_KEY должен быть случайной строкой (минимум 32 символа)
# 4. Регулярно меняйте пароли и ключи
# 5. Храните .env файл в безопасном месте
